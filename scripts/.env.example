# Worker URL for testing
# Replace with your actual Cloudflare Worker URL after deployment
WORKER_URL=http://localhost:8787

# For production:
# WORKER_URL=https://fx-crm-sync.your-subdomain.workers.dev